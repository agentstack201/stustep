# المرحلة 8 — الاختبارات الآلية وقواعد أمان Firestore

| البند | القيمة |
|---|---|
| **المطوّر** | Ahmad Hisham — أحمد هشام |
| **المرحلة** | 8 — إثبات لا ادّعاء |
| **الحالة** | ✅ مكتملة |
| **ملفات جديدة** | 8 اختبار + `firestore.rules` + `firebase.json` |
| **اختبارات الوحدة والودجت** | **153** |
| **اختبارات قواعد الأمان** | **33** على محاكي Firestore حقيقي |
| **ملفات زملاء معدَّلة** | **صفر** |

---

## 1. ماذا تم تنفيذ؟

مرحلتان في واحدة، تجمعهما فكرة واحدة: **الفرق بين «كتبت» و«أثبتُّ»**.

```
اختبارات الوحدة (95)        ←  المنطق الخالص: التطبيع · الأهلية · النقاط · الفلترة
عقود النماذج (27)           ←  التعدادات نصاً · أسبقية الشروط · التسلسل
حراس القواعد (7)            ←  قواعد CLAUDE.md نفسها كاختبارات
اختبارات الودجت (24)        ←  الشاشات تُبنى فعلاً وكل نص مترجَم
اختبارات القواعد (33)       ←  الأمان على محاكي Firestore حقيقي
```

**وكلها تُشغَّل في متصفح حقيقي أيضاً:** `flutter test --platform chrome`
ينجح بـ**127/127** — إثبات مباشر أن المنطق والنماذج تعمل مترجَمةً إلى
JavaScript، وهي المنصة التي كان يفشل عليها مسار رفع الملفات قبل إصلاحه.

---

## 2. الجزء الأول — الاختبارات الآلية

### 2.1 التوزيع

| الملف | العدد | ماذا يغطي |
|---|---|---|
| `certificate_service_test.dart` | 18 | تطبيع أربعة سلالم · صلاحية السنة · المعادلة |
| `eligibility_evaluator_test.dart` | 16 | الشروط الستة · مستويات الأهلية · أسباب الرفض |
| `major_matcher_service_test.dart` | 16 | الترتيب · الاستبعاد المُبلَّغ عنه · المحاكي |
| `matcher_scoring_test.dart` | 15 | المكوّنات الأربعة · سقف المئة |
| `housing_service_test.dart` | 15 | فلاتر السكنات الأربعة + البحث |
| `exam_paper_service_test.dart` | 15 | فلاتر النماذج · النموذج العام · السنوات المتاحة |
| `admission_models_test.dart` | 27 | التعدادات · أسبقية الشروط · التسلسل |
| `admission_architecture_test.dart` | 7 | **قواعد المشروع نفسها** كاختبارات |
| `admission_widgets_test.dart` | 19 | **رسم** الودجت بترجماتها الحقيقية |
| `widget_test.dart` | 5 | `ScoreCircle` و`EligibilityVisuals` |
| **المجموع** | **153** | |

### 2.2 لماذا كان الاختبار ممكناً أصلاً؟

**لأن المنطق فُصل عن البيانات منذ المرحلة الأولى.** هذه ليست صدفة سعيدة بل
نتيجة قرار متعمَّد:

| الوحدة | لا تعرف Firestore | تُختبر بـ |
|---|---|---|
| `CertificateService` | ✅ | 18 اختباراً |
| `EligibilityEvaluator` | ✅ | 16 اختباراً |
| `MatcherScoring` | ✅ | 15 اختباراً |
| `MajorMatcherService.rank` | ✅ | 16 اختباراً |
| `HousingService.applyFilters` | ✅ (`static`) | 15 اختباراً |
| `ExamPaperService.applyFilters` | ✅ (`static`) | 15 اختباراً |

لو كانت أيٌّ منها تجلب بياناتها بنفسها، لاحتاج اختبارها محاكياً أو اتصالاً
— فلم تكن لتُختبَر عملياً أبداً.

### 2.3 طبقة اختبارات الودجت — سدّ الثغرة الأخيرة

الاختبارات الـ95 الأولى **لا ترسم بكسلاً واحداً**. والموديول 65 ملف واجهة.
فبقي صنف كامل من الأعطال بلا تغطية:

- ودجت ترمي استثناءً عند أول رسم لأن قيمة اختيارية كانت `null`
- **مفتاح ترجمة يظهر خاماً للطالب** (`admission.housing.price`) — لا يُسقط
  التطبيق ولا يكشفه التحليل ولا يكشفه اختبار منطق

الحل: `test/admission/support/localized_pump.dart` يرسم الودجت داخل
`EasyLocalization` حقيقي بملفات الترجمة الفعلية، ثم `expectNoRawTranslationKeys()`
تمشّط كل نص على الشاشة وتفشل إن بدأ أيٌّ منها بـ`admission.`.

> **تفصيلة تقنية:** تحميل ملف الترجمة قراءة **حقيقية** من `assets`، والزمن
> المزيَّف في `pump` لا يُكملها — فتبقى الشجرة فارغة **ويمرّ الاختبار على
> لا شيء**، وهو أخطر من فشله. ولهذا يُنفَّذ أول رسم داخل `tester.runAsync`.

### 2.4 حراس القواعد المعمارية — القاعدة التي لا يحرسها اختبار تُكسر

`CLAUDE.md` يحوي ثماني قواعد حاكمة، وكانت كلها محروسة **بانضباط بشري فقط**:
تُقرأ في الوثيقة، ثم تُنسى بعد شهر أو يخالفها من لم يقرأها. وأخطرها قاعدة
«صفر `dart:io`» — كسرها **لا يُفشل التحليل ولا البناء**، ويظهر فقط كعطل
صامت للمستخدم على الويب. وهذا بالضبط ما وقع فعلاً في المرحلتين 5 و6.

`admission_architecture_test.dart` يحوّل سبعاً منها إلى اختبارات:

| # | القاعدة المحروسة | كيف تُفحص |
|---|---|---|
| 1 | لا `dart:io` في الموديول | مسح كل سطور الاستيراد |
| 2 | لا ملف فوق 300 سطر | عدّ سطور كل ملف |
| 3 | لا `print` — `debugPrint` حصراً | تعبير نمطي يستثني `debugPrint` |
| 4 | ملفّا الترجمة متطابقان مفتاحاً بمفتاح | مقارنة المجموعتين |
| 5 | كل مفتاح مستخدم موجود في اللغتين | مسح الكود كله |
| 6 | لا شاشة تستورد `cloud_firestore` | مسح كل ما عدا النماذج |
| 7 | **حارس على الحارس** | يفشل إن صار عدد الملفات الممسوحة صفراً |

> القاعدة السابعة ليست زائدة: بدونها، لو تغيّر مسار المجلد لمرّت الاختبارات
> الستة كلها على **قائمة فارغة** فبدت خضراء وهي لا تفحص شيئاً — وهو أسوأ
> أنواع الفشل لأنه يُطمئن كذباً.
>
> **وقد تُحقِّق من الحراس أنفسهم:** أُدخل ملف مخالف عمداً، ففشلت الحراس
> الثلاثة المعنية بأسماء الملفات وأرقام السطور، ثم حُذف الملف.

### 2.5 اختبار مكسور منذ أول كوميت — أُصلح

`test/widget_test.dart` المولَّد مع المشروع كان يفحص عدّاداً غير موجود
(`Counter increments smoke test`)، فيفشل `flutter test` **دائماً منذ أول
كوميت في المستودع**. رُصد في مستند التحليل كخطر رقم 9، واستُبدل بفحص ودجت
حقيقية لا تحتاج تهيئة Firebase.

---

## 3. الجزء الثاني — قواعد الأمان

### 3.1 المشكلة

**لم يكن في المشروع ملف قواعد إطلاقاً.** أي أن قاعدة البيانات على الأرجح في
وضع الاختبار المفتوح: يقرأ منها ويكتب فيها أي شخص يملك مفاتيح التطبيق
العامة — وهي مضمّنة في أي نسخة منشورة.

**والحل الساذج كان سيكون كارثة:** نشر قواعد تغطي المجموعات الجديدة فقط
**يُغلق تلقائياً كل المجموعات القائمة** (الشات، الكليات، المواد،
المستخدمين)، لأن القاعدة الافتراضية في Firestore هي المنع. النتيجة: تعطّل
التطبيق بالكامل عند كل المستخدمين فور النشر.

### 3.2 الحل — القفل التدريجي بثلاث طبقات

| الطبقة | المحتوى | القيد |
|---|---|---|
| **1** | كل مجموعة قائمة: `users` · `colleges` · `departments` · `courses` · `groups` · `messages` | تسجيل الدخول فقط — أخف قيد يبقيها تعمل |
| **2** | المرجعية الجديدة: `department_guides` · `admission_requirements` · `app_config` | قراءة للمسجَّلين · كتابة للمشرف وحده |
| **3** | محتوى المستخدمين: `housings` · `exam_papers` · `matcher_results` | ملكية صريحة + منع تغيير حالة المراجعة |

**لماذا الطبقة الأولى آمنة؟** لأنه **تُحقِّق فعلياً** أن لا شاشة في التطبيق
تقرأ من Firestore قبل المصادقة. القيد ليس تخميناً.

### 3.3 ثغرة أمنية حقيقية سُدَّت

دالة حذف الرسالة في `chat_service.dart:198` **لا تتحقق من الملكية إطلاقاً**،
والتحقق الوحيد في الواجهة (`isMe`). أي شخص يستدعي الـSDK مباشرة كان يستطيع
حذف رسائل الآخرين.

```
allow delete: if isSignedIn()
  && (resource.data.senderId == request.auth.uid || isAdmin());
```

سُدَّت **من طرف الخادم بلا تعديل ملف الشات** — وهو ملف زميل محظور — ولها
اختبار آلي يثبت سدّها.

### 3.4 قرار `admins/{uid}` بدل حقل `role`

الصلاحية الإدارية = **وجود وثيقة** باسم المستخدم في `admins`:

```
function isAdmin() {
  return isSignedIn()
    && exists(/databases/$(database)/documents/admins/$(request.auth.uid));
}
```

**لماذا؟** لسببين عمليين: تفادي تعديل `auth_service.dart` وهو ملف محظور،
وتفادي ترحيل بيانات المستخدمين الحاليين. والوثيقة تُنشأ يدوياً من الـConsole
ولا يستطيع أي عميل إنشاءها (`allow write: if false`).

### 3.5 سياسة المراجعة — ما تحرسه القاعدة فعلاً

القيمة الافتراضية لـ`moderationStatus` هي `published`، ولا توجد بعد لوحة
مشرف. اشتراط `pending` عند الإنشاء كان سيجعل كل إعلان **غير مرئي لأحد إلى
الأبد**.

ما تحرسه القاعدة: **لا يستطيع المستخدم تغيير حالة المراجعة بعد الإنشاء** —
فقرار المشرف بالرفض يثبت ولا يُلغيه صاحب المحتوى.

> الانتقال إلى مراجعة مسبقة يكلّف **تغيير كلمة واحدة**: `!= 'rejected'`
> تصير `== 'pending'` في سطر الإنشاء. بلا أي تعديل في التطبيق.

---

## 4. اختبارات القواعد — 33 اختباراً على محاكٍ حقيقي

```bash
cd test/rules && npm install && npm test
```

| # | المجموعة | ما تثبته |
|---|---|---|
| ١ | الزائر غير المسجَّل | ممنوع تماماً من كل شيء |
| ٢ | الشجرة الأكاديمية | قراءة للجميع · كتابة للمشرف وحده |
| ٣ | وثيقة المستخدم | لا تعديل لوثيقة غيرك · لا حذف إطلاقاً |
| ٤ | نتائج المطابق | خاصة بصاحبها · لا تُحرَّر |
| ٥ | الشات | **لا انتحال هوية · لا حذف رسائل الغير** |
| ٦ | محتوى المستخدمين | ملكية صريحة · لا تلاعب بحالة المراجعة |
| ٧ | المشرفون | **لا ترقية ذاتية** |
| ٨ | المنع الافتراضي | أي مجموعة غير معرَّفة ممنوعة |
| ٩ | **ضمانة عدم كسر التطبيق** | كل مجموعة يستخدمها التطبيق ما زالت مقروءة |

**المجموعة التاسعة أهمها.** هي الضمانة الوحيدة ضد السيناريو الكارثي: نشر
قواعد تُعطّل عمل الزملاء. تفشل فوراً إن أضاف أحد مجموعة بلا قاعدة.

---

## 5. القرارات الهندسية

### القرار 1 — القفل التدريجي لا الشامل

**Decision:** تغطية كل مجموعة قائمة صراحةً بأخف قيد يبقيها تعمل.

**Reason:** الحل الساذج يُعطّل التطبيق عند كل المستخدمين لحظة النشر.

**Alternative:** قواعد للجديد فقط — أقصر بثلاثين سطراً، وتُسقط التطبيق.
مرفوض قطعاً.

### القرار 2 — قاعدة المنع الافتراضي مكتوبة صراحةً

**Decision:** `match /{document=**} { allow read, write: if false; }`

**Reason:** غير ضرورية تقنياً (المنع هو الأصل) لكنها **توثيق مقصود**: من
يضيف مجموعة جديدة تفشل كتابته فوراً فينتبه لوجوب إضافة قاعدتها.

### القرار 3 — `referenceYear` قابلة للحقن في كل ما يعتمد الوقت

**Decision:** كل دالة تحسب عمر الشهادة تقبل سنة مرجعية اختيارية.

**Reason:** اختبار يعتمد على ساعة النظام ينكسر في أول يناير بلا أي تغيير في
الكود. الاختبار غير المستقر أسوأ من غيابه لأنه يُفقد الثقة بالمجموعة كلها.

### القرار 4 — اختبار «سقف المئة»

**Decision:** اختبار يثبت أن أفضل حالة ممكنة لا تتجاوز 100.

**Reason:** الأوزان أربعة ثوابت منفصلة. تعديل أحدها مستقبلاً بلا انتباه
يكسر معنى النتيجة كلها. هذا الاختبار حارس على الثوابت لا على الكود.

### القرار 5 — فحص المفاتيح الخام في اختبارات الودجت

**Decision:** `expectNoRawTranslationKeys()` على كل شاشة تُرسم.

**Reason:** المفتاح غير المترجَم لا يُسقط التطبيق ولا يكشفه التحليل، بل
يظهر للطالب كنص تقني مبهم. هذا الفحص أرخص طريقة لسدّ الصنف كله دفعة واحدة.

---

## 6. أوامر التحقق الكاملة

```bash
export PATH="/opt/fl/flutter/bin:$PATH"

flutter analyze                            # صفر أخطاء
flutter test --reporter=failures-only      # 153 / 153
flutter test --platform chrome test/       # 127 / 127 في متصفح حقيقي
cd test/rules && npm install && npm test   #  33 / 33
flutter build web --release --no-web-resources-cdn   # يبنى بنجاح
```

> **لماذا `--no-web-resources-cdn`؟** بدونها يُجلب CanvasKit من
> `gstatic.com` وقت التشغيل، فلا يعمل التطبيق إطلاقاً على شبكة تحجب ذلك
> النطاق — وشبكات الجامعات تفعل ذلك كثيراً. سكربت Vercel يستخدمها.

---

## 7. قبل النشر إلى الإنتاج

```bash
cd test/rules && npm install && npm test    # يجب أن تنجح 33/33 أولاً
firebase login
firebase emulators:start --only firestore   # جرّب التطبيق كاملاً على المحاكي
# أنشئ وثيقة admins/{uid} لحسابك يدوياً من Console
firebase deploy --only firestore:rules
```

> ⚠️ **لا تنشر قبل تجربة المحاكي.** القواعد تشمل مجموعات زملائك.
> والكتابة من Firebase Console تتجاوز القواعد دائماً، فإدخال البيانات
> الأكاديمية يدوياً يبقى ممكناً كما هو.

---

## 8. أسئلة المناقشة المتوقعة

**س: هل جرّبت الكود فعلاً؟**
نعم. `flutter analyze` صفر أخطاء، و`flutter test` يشغّل 153 اختباراً كلها
ناجحة، و**127 منها تُشغَّل في متصفح Chromium حقيقي**، واختبارات القواعد
تعمل على محاكي Firestore حقيقي، والتطبيق يُبنى للويب بنجاح. وأول تشغيل للتحليل كشف أن **المشروع لم يكن يبنى أصلاً** بأربعين
خطأ، وكلها أُصلحت.

**س: ما أخطر مشكلة اكتشفتها؟**
ثلاث: أن المشروع لا يبنى إطلاقاً، وأن قاعدة البيانات بلا أي قواعد أمان،
وأن أي مستخدم يستطيع حذف رسائل غيره في الشات. الثلاث عولجت، والثالثة لها
اختبار آلي يثبت سدّها.

**س: لماذا لم تنشر القواعد مباشرة؟**
لأنها تشمل مجموعات زملائي. نشرها بلا تجربة على المحاكي يخاطر بتعطيل عملهم،
ولذلك كتبت اختباراً حارساً يتأكد أن كل مجموعة يستخدمها التطبيق ما زالت
مقروءة بعد التطبيق.

**س: كيف تختبر واجهة بلا تشغيل التطبيق؟**
أرسم الودجت وحدها داخل بيئة الترجمة الحقيقية وأفحص ما ظهر. لا يغني عن
التشغيل على جهاز، لكنه يمسك صنفين لا يمسكهما التشغيل اليدوي بموثوقية:
الاستثناء عند أول رسم، والمفتاح غير المترجَم.

---

## 9. الحالة

✅ **المرحلة 8 مكتملة — والموديول كله (0 → 8) مكتمل.**

المتبقي كله خارج الكود: نشر القواعد على مشروع Firebase الحقيقي، والتشغيل
على جهاز أمام الدكتور. التفاصيل في `docs/NEXT_STEPS.md`.
